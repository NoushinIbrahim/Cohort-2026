const carriage1 = ['zunni', 'miahu', 'noshi']
const emptyCarriage  = []

const threeEmptySeats = Array(3) //it will show how many item we have in array right now we have 3 but this is empty array
console.log(threeEmptySeats.length);

const passenger = Array['zoii', 'zunii', 'zunair'] //in this one we have values

const trainCode = Array.from('dust') //it will create a array from dust  like this['d', 'u', 's', 't']
console.log(trainCode);

const tempTrain = ['a', 'b', 'c', 'd', 'e'] 
tempTrain.length = 3 //ab array ke element only 3 hongy
console.log(tempTrain); //result is a,b,c
tempTrain.length=5 // ab last 2 items empty hongy or length 5 hogi
console.log(tempTrain);
g

// some array methods
//push =>add value in the end of array
//pop => remove the value from end
//unshift => add value in the start
//shift => remove value from start
//splice =>

    //concat, slice, flat =>thy will return a new array without touching orignal one

    const trainCopy = wholeTrain.slice() //it will copy the data

    //searching command: indexOf, includes, find, findIndex

    console.log(typeof []); //it will show u this is obejct
    console.log(Array.isArray('zoii'));

    //key points array baiscs
    //1. [] array(n)
    //2. array are 0 based
    //3. mutatung methods: push, pop, shift, splice, unshift => they will change original array
    //4. non mutating: concat, slice, flat , flatMap =>thy will return a new array without touching orignal one
    //5. searching includes
    //6. array.isArray()
    




