const crewMembers= 40
const fuel= 142.43
const light_speed = 299_888_999

const infiniteRange = Infinity
const negativeInfiniteRange = -Infinity
const notANumber = NaN
'4'
'zunni' //these are NaN because u cant convert them into number
'mishu' //these are NaN because u cant convert them into number

console.log(1 /0); //the result is infinity
console.log(-1 /0); //the result is negitive infinity

const thristForve = 3.545

console.log(Math.round(thristForve));
console.log(Math.floor(thristForve));
console.log(Math.ceil(thristForve));
console.log(Math.trunc(thristForve));


const temps = [-19, 54, 65, 79]

console.log(Math.max(temps));
console.log(Math.min(temps));
